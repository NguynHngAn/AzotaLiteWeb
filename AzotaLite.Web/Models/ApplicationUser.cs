using Microsoft.AspNetCore.Identity;

namespace AzotaLite.Web.Models
{
    public class ApplicationUser : IdentityUser
    {
        // Chỗ này bạn có thể bổ sung thuộc tính sau này (FullName, TenantId, ...)
    }
}
